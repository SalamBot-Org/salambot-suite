import { aiLangDetectLangDetect } from './ai-lang-detect-lang-detect';

describe('aiLangDetectLangDetect', () => {
  it('should work', () => {
    expect(aiLangDetectLangDetect()).toEqual('ai-lang-detect-lang-detect');
  });
});
